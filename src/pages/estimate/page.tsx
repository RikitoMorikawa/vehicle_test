// src / pages / estimate / page.ts;
import React from "react";
import EstimateContainer from "../../containers/estimate/page";

const EstimatePage: React.FC = () => {
  return <EstimateContainer />;
};

export default EstimatePage;
