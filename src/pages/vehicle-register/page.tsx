// src / pages / vehicle - register / page.tsx;
import React from "react";
import VehicleRegisterContainer from "../../containers/vehicle-register/page";

const VehicleRegisterPage: React.FC = () => {
  return <VehicleRegisterContainer />;
};

export default VehicleRegisterPage;
