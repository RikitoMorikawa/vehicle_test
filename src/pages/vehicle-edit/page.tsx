// src / pages / vehicle - edit / page.tsx;
import React from "react";
import VehicleEditContainer from "../../containers/vehicle-edit/page";

const VehicleEditPage: React.FC = () => {
  return <VehicleEditContainer />;
};

export default VehicleEditPage;
